CommandPalette {
    align: center middle;
}

#palette-panel {
    width: 60;
    height: auto;
    max-height: 20;
    padding: 1 2;
    border: solid $primary;
    background: $surface;
}

#search-input {
    margin-bottom: 1;
}
